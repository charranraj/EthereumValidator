import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <nav className="header">
      <h1>Ethereum Validator Tracker</h1>
    </nav>
  );
};

export default Header;
